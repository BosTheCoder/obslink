{
  "name": "obslink",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Turn obsidian:// URIs into https links that work everywhere",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
